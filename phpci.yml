build_settings:
    verbose: false
    prefer_symlink: false
    ignore:
        - "vendor"
        - "tests"

setup:

test:
    codeception:
        config: "tests/codeception/"
        path: "tests/codeception/_output/"

complete:
