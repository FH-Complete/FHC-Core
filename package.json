{
  "scripts": {
    "noten:check": "node tests/cypress/tools/dbCheck.js noten",
    "noten:api": "cypress run --spec \"tests/cypress/e2e/specs/{unit,api}/**/*.cy.js\"",
    "noten:ui": "cypress run --browser chrome --spec \"tests/cypress/e2e/specs/ui/**/*.cy.js\""
  },
  "//": [
    "pg: the suite resets and seeds the test database directly (tasks/db.js, tools/dbCheck.js)",
    "dotenv: cypress.config.js and the suite read URL, accounts and DB access from the .env files"
  ],
  "dependencies": {
    "cypress": "^15.21.1",
    "dotenv": "^17.4.2",
    "pg": "^8.13.1"
  }
}
